spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
  h2:
    console:
      enabled: false
  mail:
    host: localhost
    port: 25
    properties:
      mail:
        smtp:
          auth: false
          starttls:
            enable: false
          # Timeouts agregados para evitar fallos por bloqueo en GitHub Actions
          connectiontimeout: 1000
          timeout: 1000
          writetimeout: 1000

jwt:
  secret: claveSecretaDePruebaSuperSeguraYAlfanumericaParaElBootcamp2026
  expiration: 3600000

app:
  mail:
    from: noreply@test.com
  url: http://localhost:8080

frontend:
  url: http://localhost:5173